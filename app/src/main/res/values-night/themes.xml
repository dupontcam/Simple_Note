<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SimpleNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tema Escuro -->
        <item name="colorPrimary">@color/blue_grey_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/blue_grey_500</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:colorControlNormal">@color/white</item> <!-- Ícones do menu -->
    </style>

    <!-- Estilo para o FloatingActionButton no tema escuro -->
    <style name="FabStyleDark" parent="Widget.Design.FloatingActionButton">
        <item name="backgroundTint">@color/blue_grey_700</item>
        <item name="rippleColor">@color/black</item>
        <item name="android:tint">@color/white</item> <!-- Ícone do FAB -->
    </style>
</resources>
